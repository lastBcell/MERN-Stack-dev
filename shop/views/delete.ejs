<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Confirmation</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- <style>
        body {
            background-color: blue;
            color: white;
        }
    </style> -->
</head>
<body class="bg-primary">
    <div class="container py-5 w-50 ">
        <div class="card  ">
            <div class="card-header bg-danger text-white">
                <h1>Delete Confirmation</h1>
            </div>
            <div class="card-body">
                <p class="fs-5">Are you sure you want to delete the product below?</p>

                <p><strong>Name:</strong> <%= product.productName %></p>
                <p><strong>Description:</strong> <%= product.productDescription %></p>
                <p><strong>Price:</strong> <%= product.productPrice %> Rs</p>

                <form action="/delete/<%= product._id %>" method="post">
                    <button type="submit" class="btn btn-danger btn-lg me-2">Yes, Delete</button>
                    <a href="/" class="btn btn-secondary btn-lg">No, Cancel</a>
                </form>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS (optional, for interactive components) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
